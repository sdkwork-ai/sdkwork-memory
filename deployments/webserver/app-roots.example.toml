# Adaptive Web roots for sdkwork-memory (copy into runtime config when in-process).
# Docker entrypoint generates /etc/sdkwork/webserver/module-app-roots/sdkwork-memory.toml

[app_roots]
tablet_surface = "pc"

[app_roots.pc_static_by_environment]
development = "apps/sdkwork-memory-pc/dist/standalone/dev"
test = "apps/sdkwork-memory-pc/dist/standalone/test"
staging = "apps/sdkwork-memory-pc/dist/standalone/staging"
demo = "apps/sdkwork-memory-pc/dist/standalone/demo"
production = "apps/sdkwork-memory-pc/dist/standalone/prod"

[app_roots.static_fallback_by_environment]
development = "deployments/webserver/static"
test = "deployments/webserver/static"
staging = "deployments/webserver/static"
demo = "deployments/webserver/static"
production = "deployments/webserver/static"
