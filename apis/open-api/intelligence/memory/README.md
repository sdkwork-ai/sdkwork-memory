# Open API domain workspace for SDKWork Memory (`intelligence/memory`).

Place route manifests, schemas, examples, changelogs, and validation fixtures here as HTTP implementation expands.

Current authority OpenAPI: `apis/open-api/memory-open-api.openapi.json` (generated by `node tools/materialize_phase1_contracts.mjs`). `sdks/sdkwork-memory-sdk/openapi/memory-open-api.openapi.json` is a byte-identical generation-input mirror of this authority, not the other way around.
